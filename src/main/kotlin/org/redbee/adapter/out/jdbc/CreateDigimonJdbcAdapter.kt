package org.redbee.adapter.out.jdbc

import org.redbee.application.port.out.CreateDigimonRepository
import org.redbee.application.port.out.GetDigimonByNameRepository
import org.redbee.application.usecase.model.Digimon

class CreateDigimonJdbcAdapter : CreateDigimonRepository {
    override fun execute(digimon: Digimon) {

    }
}